#!/bin/bash
# 1. install the virtual environment
python3 -m venv .todolist_env
# 1. activate the environment: source ./.todolist_env/bin/activate
echo "1. activate the environment:source ./.todolist_env/bin/activate"
echo "2. start the setuptool integration: pip install --editable ."
echo "after that run this command to test: todolist --help "
